import { Box } from "@mui/material";

const AppBar = () => {
  return (
    <Box
      style={{
        width: "100%",
        height: "10vh",
        backgroundColor: "gray",
      }}
    >
      appBar
    </Box>
  );
};

export default AppBar;
